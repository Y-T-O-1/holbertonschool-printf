#include "main.h"

/**
 * print_percent - Print % symbol
 * @percent: Argument passed to us (although it's unused in this function)
 * Return: 1 if we're good, Else if not.
 */
int print_percent(__attribute__((unused))va_list percent)
{
	_putchar("%");
	return (1);
}
